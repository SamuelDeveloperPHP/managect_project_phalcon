<!doctype html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Cadastro dev · Gestao de Projetos</title>
    <link rel="stylesheet" href="/assets/app.css">
    <link rel="stylesheet" href="/assets/auth.css">
</head>
<body class="auth-body">
    <main class="auth-shell auth-shell-wide">
        <section class="auth-visual auth-visual-compact" aria-label="Planejamento de tarefas">
            <div class="auth-brand">
                <span class="auth-brand-mark">P</span>
                <div>
                    <strong>Novo workspace</strong>
                    <small>Ambiente de desenvolvimento</small>
                </div>
            </div>

            <div class="auth-kanban">
                <div>
                    <strong>Backlog</strong>
                    <span>5</span>
                </div>
                <div>
                    <strong>Em andamento</strong>
                    <span>3</span>
                </div>
                <div>
                    <strong>Concluido</strong>
                    <span>12</span>
                </div>
            </div>

            <div class="auth-mini-chart">
                <span style="height: 38%"></span>
                <span style="height: 62%"></span>
                <span style="height: 48%"></span>
                <span style="height: 78%"></span>
                <span style="height: 56%"></span>
            </div>
        </section>

        <section class="auth-panel auth-register-panel" aria-labelledby="register-title">
            <p class="eyebrow">Somente desenvolvimento</p>
            <h1 id="register-title">Criar cadastro inicial</h1>
            <p class="muted">Crie uma empresa de teste e o usuario administrador para acessar o sistema.</p>

            <div class="flash">
                <?= $this->flashSession->output() ?>
            </div>

            <form class="auth-form" action="/register" method="post">
                <input type="hidden" name="csrf_token"
                    value="<?= htmlspecialchars($csrfToken, ENT_QUOTES, 'UTF-8') ?>">

                <div class="auth-form-grid">
                    <div class="auth-field">
                        <label for="company_name">Workspace</label>
                        <input id="company_name" name="company_name" type="text"
                            autocomplete="organization" placeholder="Nexocore Tecnologia" required autofocus>
                    </div>

                    <div class="auth-field">
                        <label for="domain">Dominio</label>
                        <input id="domain" name="domain" type="text"
                            value="<?= htmlspecialchars((string)$defaultDomain, ENT_QUOTES, 'UTF-8') ?>"
                            placeholder="empresa.com.br" required>
                    </div>

                    <div class="auth-field">
                        <label for="name">Administrador</label>
                        <input id="name" name="name" type="text"
                            autocomplete="name" placeholder="Samuel Melo" required>
                    </div>

                    <div class="auth-field">
                        <label for="email">E-mail</label>
                        <input id="email" name="email" type="email"
                            autocomplete="username" placeholder="admin@empresa.com.br" required>
                    </div>

                    <div class="auth-field">
                        <label for="password">Senha</label>
                        <input id="password" name="password" type="password"
                            autocomplete="new-password" minlength="8" placeholder="Minimo 8 caracteres" required>
                    </div>

                    <div class="auth-field">
                        <label for="password_confirmation">Confirmar senha</label>
                        <input id="password_confirmation" name="password_confirmation" type="password"
                            autocomplete="new-password" minlength="8" placeholder="Repita a senha" required>
                    </div>
                </div>

                <button class="auth-submit" type="submit">Criar ambiente</button>
            </form>

            <p class="auth-alt">
                Ja tenho acesso.
                <a href="/login">Voltar para login</a>
            </p>
        </section>
    </main>
</body>
</html>
