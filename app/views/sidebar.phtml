<aside class="sidebar">
    <nav class="sidebar-nav" aria-label="Navegacao principal">
        <p class="nav-label">Menu</p>

        <a class="nav-item <?= ($currentPage ?? '') === 'dashboard' ? 'active' : '' ?>" href="/dashboard">
            <span class="nav-icon">D</span>
            <span>Dashboard</span>
        </a>

        <a class="nav-item <?= ($currentPage ?? '') === 'projects' ? 'active' : '' ?>" href="/projects">
            <span class="nav-icon">P</span>
            <span>Projetos & Gantt</span>
        </a>

        <?php if (($auth['role'] ?? '') === 'admin' || ($auth['role'] ?? '') === 'master'): ?>
            <a class="nav-item <?= ($currentPage ?? '') === 'users' ? 'active' : '' ?>" href="/users">
                <span class="nav-icon">U</span>
                <span>Usuários</span>
            </a>

            <a class="nav-item <?= ($currentPage ?? '') === 'companies' ? 'active' : '' ?>" href="/companies/profile">
                <span class="nav-icon">E</span>
                <span>Dados da Empresa</span>
            </a>
        <?php endif; ?>
    </nav>

    <form class="sidebar-logout" action="/logout" method="post">
        <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrfToken ?? '', ENT_QUOTES, 'UTF-8') ?>">
        <button class="btn btn-logout" type="submit">Sair do sistema</button>
    </form>
</aside>
